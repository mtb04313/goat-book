server {
    listen 80;
    server_name DOMAIN;

    location /static {
        alias /home/ubuntu/sites/goat-book-staging-server/static;
    }

    location / {
        proxy_pass http://unix:/tmp/goat-book-staging-server.socket;
        proxy_set_header Host $host;
    }
}

